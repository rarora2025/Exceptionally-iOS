# Supabase — copy to .env and fill in from Supabase → Project Settings → API.
# These are PUBLIC (anon key is safe in a client app; RLS protects data).
EXPO_PUBLIC_SUPABASE_URL=https://YOUR-PROJECT-ref.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=YOUR-ANON-PUBLIC-KEY
